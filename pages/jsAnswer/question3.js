function Solvetion(arr) {
  if (!Array.isArray(array) || array.length === 0) {  
    throw new Error('Please provide a non-empty array');   
  }  
  return Math.max(arr);  
}
  

// let arr = [1, 2, 3, 4, 5];  
// console.log(Solvetion(arr));  // 输出：5
